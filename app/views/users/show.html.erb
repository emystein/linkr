<div id="bookmarks-header">
  <h2><%= @user.nickname %>'s Bookmarks</h2>
  <%= link_to 'Import', '/bookmarks/import', class: 'btn btn-warning' %>
  <%= render 'components/search', search_path: user_path %>
  <hr>
</div>

<section>
  <% unless @bookmarks.empty? %>
    <%= render :partial => 'bookmarks/bookmark', :collection => @bookmarks %>
    <%= will_paginate @bookmarks %>
  <% else %>
    <h4>There are no bookmarks to display.</h4>
  <% end %>
</section>

<section>
  <%= link_to 'Tags', user_path + '/tags' %>
</section>